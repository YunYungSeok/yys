<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" 
xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
xsi:schemaLocation="http://www.springframework.org/schema/beans 
http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">
	   <!-- java에서 import와 비슷함  -->
	   
	   <bean id="score" class="exam.test01.Score"/>	   
	   <!-- 네임스페이스를 정의한 xml 스키마 파일의 위치를 지정 
	   		자바코드로 표현하면 new exam.test01.Score();
	   		스프링 Ioc 컨테이너가 생성할 자바 빈에 대한 정보는 <bean>태그로 선언한다.
	   		클래스 속성엔 클래스 이름을 지정한다.패키지 이름을 포함한 풀네임이여야한다.
	   		id속성은 객체의 식별자다.빈 컨테이너에서 객체를 꺼낼 때 이 식별자를 사용한다. 
				   		
	   	-->
	   
	   
</beans>